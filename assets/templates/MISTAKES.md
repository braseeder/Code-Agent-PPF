# Mistakes ledger

Required reading at the start of **every** workload. Each entry is a rule paid for with a real defect — repeating one means the rule was written badly, so rewrite it sharper, don't just re-log it.

Write rules so the mistake is recognizable *before* it happens again: name the situation, not just the error.

## Rules

<!-- newest first; one line each, expand only if the one-liner can't carry it -->

| # | Date | Rule (when X, do/never Y) | Caught by | Recurrences |
|---|---|---|---|---|
| 1 | {date} | {e.g. "When accepting a path from input, resolve and check it against the base dir — traversal slipped through in workload 2"} | {security / alignment / nitpick / human} | 0 |
